{
  "name": "discordbot",
  "version": "1.0.0",
  "description": "bot for pcparadise",
  "main": "dist/main.js",
  "scripts": {
    "start": "tslint -c tslint.json 'src/**/*.ts' && tsc && node dist/main.js",
    "check": "tslint -c tslint.json 'src/**/*.ts' && prettier --check .",
    "format": "prettier . --write"
  },
  "author": "Arvinder Dhanoa",
  "license": "MIT",
  "devDependencies": {
    "discord-api-types": "^0.23.1",
    "prettier": "^2.4.1",
    "tslint": "^6.1.3",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "discord.js": "^13.2.0"
  }
}
